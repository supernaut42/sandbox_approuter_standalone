{
  "name": "sandbox_approuter_standalone",
  "version": "1.0.0",
  "description": "Sandbox for CAP app to be deployed as Standalone Approuter",
  "repository": "https://github.com/supernaut42/sandbox_approuter_standalone",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "express": "^4",
    "@sap/xssec": "^3",
    "passport": "^0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^7"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "[hybrid]": {
        "auth": "xsuaa"
      },
      "[production]": {
        "auth": "xsuaa"
      }
    }
  }
}
